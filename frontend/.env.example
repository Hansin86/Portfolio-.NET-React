# Base URL of the PortfolioApp API. Vite inlines VITE_* vars at build time,
# so each environment needs its own value (and a rebuild to change it).
# Local API default (see docs/implementation-plan.md): http://localhost:5029
VITE_API_BASE_URL=http://localhost:5029
