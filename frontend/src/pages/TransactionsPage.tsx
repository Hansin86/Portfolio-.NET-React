/** Placeholder. The filtered/sorted/paged list and create/edit/delete land in the
 *  Transactions list + create/edit/delete commits. This confirms the protected
 *  route renders inside the authenticated layout. */
export function TransactionsPage() {
  return (
    <div>
      <h1>Transactions</h1>
      <p>The transactions list arrives in an upcoming commit.</p>
    </div>
  )
}
