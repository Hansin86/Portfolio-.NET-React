# Local PostgreSQL credentials consumed by docker-compose.yml.
# Copy this file to .env and adjust the values. .env is gitignored.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=portfolio
